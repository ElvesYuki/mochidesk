# MochiDesk 待办

## Backlog

- 为真实系统监控能力预留更稳定的数据接口，替换当前纯模拟实现
- 明确动画映射层的数据输入和展示输出，减少组件内临时计算
- 评估桌宠状态流需要哪些最小状态，不提前引入复杂状态机
- 为手部动作、道具和电脑操作预留更明确的输入模型
- 清理模板和无引用资源，保持仓库骨架干净

## Todo

- 持续同步 `README.md`、`AGENTS.md` 和 `docs/memory/`，避免现状与文档漂移
- 为平台相关能力建立最小验证清单，例如透明窗口、置顶、拖拽、吸附、屏幕边界修正
- 继续评估是否拆分 `MochiAvatar.svelte` 的 SVG 结构与样式块，避免后续手部动作和道具继续堆积
- 持续校准真实监控链路的刷新频率、平滑参数和错误回退，平衡响应速度与稳定感
- 将 Codex 真实运行状态接入 `src-tauri/src/monitor/`，替换当前占位接口并补足对应动画反应
- 按 `docs/plans/codex-状态来源与桌宠行为规划.md` 推进第一条真实 Codex 状态来源链路，优先评估本地自动发现方案
- 按 `docs/plans/局域网通信与受控执行能力规划.md` 推进阶段 1，先建立 UDP 最小收发、peer 发现与最近消息链路
- 按 `docs/plans/局域网通信与受控执行能力规划.md` 推进“Mac 发部署请求、Windows 拉代码并重启服务”的第一版闭环
- 为接收端设计本地 deployment profile，收口项目、分支、机器标识、共享密钥和服务任务映射
- 为部署链路补齐 `deploy_announce / deploy_ack / deploy_result` 的最小消息模型与结果回传
- 基于 `Local Auto` 继续补齐 `waiting_input`、`error` 和多会话优先级识别
- 继续细化 `waiting_input` 的真实来源，区分“刚完成后等待下一句”和“需要确认/审批”

## Doing

- 暂无

## Done

- 2026-04-10 为 Trigger 卡片增加 `Ready to send / Blocked` 状态条，并支持直接测试 trigger，不必先跑完整任务链
- 2026-04-10 为手动发送与 trigger 增加发送前预检查，并给接收端 profile 增加启用开关，禁用 profile 时会拒绝执行远程技能
- 2026-04-10 控制面板新增“打开配置目录 / 文件”入口，并为发送配置与接收端 profile 加入基础缺项提示
- 2026-04-10 控制面板新增统一配置文件路径显示，并让 `Triggers / Outbound skill configs / Profiles` 支持从 UI 新增与删除
- 2026-04-10 将发送侧 `Triggers / Outbound skill configs` 与接收端 `Profiles` 接入 Tauri 应用配置目录下的统一 JSON 文件，重启后配置不再丢失
- 2026-04-10 新增接收端 Receiver Profiles 面板，支持从 UI 编辑 `machineId / branch / token / 路径 / 任务映射` 并写回 Rust 内存状态
- 2026-04-10 让控制面板中的发送侧技能配置与 Trigger 规则支持直接编辑，当前先持久化在本地 localStorage
- 2026-04-10 新增发送侧远程技能配置与第一版 Trigger 面板，支持手动发送和本地任务成功后自动广播 `deploy-project`
- 2026-04-10 为远程技能链路补齐 `deploy_progress` 广播、按 `requestId` 聚合状态，并让控制面板的 Skills 视图直接显示分类 / 步骤 / 成败
- 2026-04-01 修复 Codex 状态链路的首轮 review 问题，收紧 `celebrate` 匹配、为原生轮询加入缓存，并让 Codex 调试循环正确显示信息面板
- 2026-03-31 按 `AGENTS-bootstrap-pack.md` 补强 `AGENTS.md` 的通用工程协作规则，并与 MochiDesk 当前现状对齐
- 2026-03-31 为 Codex 状态补充 `notice / celebrate / error_burst` 三类细分事件态，并接入本地调试循环与第一版原生关键词识别
- 2026-03-31 继续拉开 Codex 行为差异，让 `acting` 更像左右手错峰敲击、`waiting_input` 更像抬头停住等回应
- 2026-03-31 增加独立的 Codex 调试状态循环，可本地轮播 `idle / thinking / acting / waiting_input / error / done`
- 2026-03-31 基于真实本地 rollout 样本补上 `error` 与 `waiting_input` 的第一版识别规则，并让 `done` 短暂停留后自然回落
- 2026-03-31 接入第一版 `Local Auto` Codex 状态读取，在 Tauri runtime 下扫描本机 `~/.codex/sessions/**/*.jsonl` 并映射 `idle / thinking / acting / done`
- 2026-03-31 新增 `docs/plans/codex-状态来源与桌宠行为规划.md`，整理状态来源、聚合模型和后续功能规划池，避免后续想法丢失
- 2026-03-31 增加 `CodexStatus` 模型、`codex-monitor` service 与 Rust 占位命令，先打通后续接入真实 Codex 监听的边界
- 2026-03-31 收紧桌宠窗口外围留白，将窗口从 `90 x 108` 调整为 `82 x 94`，头像本体尺寸保持不变，并额外裁掉一部分底部空区
- 2026-03-31 增加本地调试用的模式循环开关，让 `idle / calm / alert / busy` 每档约 5 秒轮播，方便直接观察状态差异
- 2026-03-31 为持续高负载状态补上低频“小循环烦躁动作”，让红温更像长期待不住
- 2026-03-30 为悬停链路补上轻量“离开后松一口气”收尾，并让红温态 hover 更烦躁一点
- 2026-03-30 为桌宠加入轻量“靠近悬停预备反应”，鼠标进入附近时会短暂警觉一下再自然回落
- 2026-03-30 为桌宠加入拖拽时的“被拎起姿态”，补齐待机 / 点击 / 拖拽三段生命感
- 2026-03-30 为点击反馈加入短暂“看向点击点”的注视反应，让桌宠更像在回应用户
- 2026-03-30 为点击反馈加入整体压缩/弹回，并按轻载、警觉、红温分化点击反应
- 2026-03-30 为桌宠增加点击短促反馈，点击时会触发表情收紧与手部抬动
- 2026-03-30 将桌宠状态展示拉开为轻载、警觉、红温三段，并放大手部让姿态区别更明显
- 2026-03-30 将 MochiAvatar 的 SVG 结构按本体、手部、表情、热态效果拆成局部组件，降低后续迭代耦合
- 2026-03-30 拆出 MochiAvatar 的造型路径与样式变量计算到 feature 内部模块，降低单文件复杂度
- 2026-03-30 为真实监控链路加入“快起慢落”的平滑与小幅抖动死区，减少瞬时 CPU 波动导致的情绪跳变
- 2026-03-30 完成 CPU / 内存共同驱动的动作映射，并让 `MotionProfile` 的配色、缩放和浮动参数进入头像表现
- 2026-03-30 打通真实系统监控最小链路：Rust 命令返回 CPU / 内存快照，前端 service 在 Tauri runtime 轮询并替换纯模拟实现
- 2026-03-30 修复 macOS 下桌宠窗口白底问题，启用 `app.macOSPrivateApi` 并补充页面根节点透明背景
- 2026-03-25 拆出 `HomePage.svelte` 中的窗口位置管理、拖拽和边缘吸附逻辑到 `src/lib/services/pet-window.ts`
- 2026-03-25 完成桌宠窗口的实时位置保存、离屏修正、多显示器恢复和边缘吸附能力
- 2026-03-25 完成像素风小木藕桌宠主体，包含连续旋转豆芽、红温蒸汽 / 融化表现和可见豆豆手
- 2026-03-25 拉长模拟系统状态持续时间，降低状态切换急促感
- 2026-03-25 完成 `AGENTS.md` 的 MochiDesk 化迁移，补充工程原则、检查清单、提交规范和记忆协作规约
- 2026-03-25 初始化项目记忆目录结构和基础文档
