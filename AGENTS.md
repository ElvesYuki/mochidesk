# MochiDesk 项目说明

## 项目定位

MochiDesk 是一个基于 `Tauri 2 + SvelteKit 2 + Svelte 5 + TypeScript + Rust` 的桌面应用项目。

当前仓库的目标是维护一个干净、可扩展的桌面应用骨架，并逐步推进“小木藕”桌面宠物 / 灵动组件方向的 MVP。不要沿用其他项目里的 Python、Vue、FastAPI、Pinia、SQLAlchemy 等规范，也不要把企业 CRUD 项目的分层和命名习惯直接搬进来。

## 当前技术栈

- 前端：`SvelteKit`、`Svelte 5`、`TypeScript`、`Vite`
- 桌面壳：`Tauri 2`
- 原生侧：`Rust`

## 目录约定

- `src/`：SvelteKit 前端代码
- `src/routes/`：页面路由
- `src/lib/components/`：通用 Svelte 组件
- `src/lib/features/`：按特性组织的界面和交互
- `src/lib/services/`：前端服务层，负责封装原生调用、系统数据适配和平台能力接入
- `src/lib/animation/`：动画参数、状态映射和动效模型
- `src/lib/models/`：前端数据模型和类型定义
- `src/lib/utils/`：纯工具函数
- `src-tauri/`：Tauri 与 Rust 代码
- `src-tauri/src/commands/`：Tauri 命令
- `src-tauri/src/window/`：窗口能力封装
- `src-tauri/src/monitor/`：系统监控能力或占位实现
- `static/`：静态资源
- `docs/memory/`：项目长期记忆、待办、决策和会话纪要

## 开发约束

- 只保留与当前项目直接相关的代码，及时删除模板示例、废弃资源和未使用的桥接逻辑
- 前端优先保持简单清晰，避免把页面、状态、原生调用和样式长期堆在同一个文件里
- 新增 Tauri 命令时，在 `src-tauri/src/lib.rs` 中集中注册，并只保留实际需要的 capability 和插件
- 不要把其他项目里的开发规范、脚手架结构或技术选型直接搬进来
- 当前阶段优先服务桌面端 MVP，不提前引入 monorepo、移动端壳、复杂状态机或与现阶段无关的基础设施
- 文档必须明确区分“当前已实现”和“未来规划”，不要把规划写成现状

## 工程原则

### 简洁化与修复原则

- 保持项目简洁，优先删除无引用代码、备份文件、重复逻辑、历史兼容壳和无价值判断
- 遇到问题先分析根因，优先做源头修复，不要在未理解问题前直接加条件分支掩盖症状
- 新增判断必须有明确理由，只能用于真实边界、必要容错或明确的兼容场景
- 必要复杂度可以保留，但必须集中管理、职责清晰，避免复杂度向上层泄漏
- 同一职责尽量只有一个主入口，避免多处重复实现或上层直接依赖底层细节
- 代码、测试、文档要同步一致，避免逻辑已变但说明和验证未更新

### 分层与边界原则

- 界面展示优先放在 `features` 或 `components`，不要让页面入口长期承担具体业务细节
- 原生能力接入优先经过 `src/lib/services/` 封装，不要在多个组件里散落 Tauri API 调用
- 动画参数、状态到表现的映射优先放在 `src/lib/animation/`，不要把大量动效计算直接塞进页面模板
- 监控数据模型、展示模型和视觉状态应尽量分离，避免把“系统数据”“动画参数”“组件细节”耦合成一层
- Rust 侧窗口能力、监控能力和命令注册按模块组织，避免都堆在入口文件

### MVP 与演进原则

- 优先做桌面端 MVP，再决定何时扩展 Web 端与移动端
- 优先定义数据模型和动画模型，再扩展多端 UI 壳
- 优先抽象接口边界，再引入跨平台实现
- 优先轻量和流畅，再增加装饰性能力
- 只有在共享逻辑、共享组件和共享配置已经出现明确复用价值时，才考虑升级为 monorepo

## 快速检查清单

在生成代码前，请先确认以下内容：

1. 读取 `docs/memory/index.md` 和 `docs/memory/待办.md`，掌握当前进度、阶段目标和硬约束
2. 涉及具体方案或历史决策时，再按需检索 `docs/memory/决策/`、`docs/memory/方案/` 和 `docs/memory/会话纪要/`
3. 确认改动是否服务当前桌面 MVP，而不是提前实现未来规划
4. 确认是否可以通过删除或抽离解决问题，而不是继续叠加兼容分支
5. 确认新增代码是否符合当前目录边界，不把页面、状态、原生调用和动画逻辑继续堆在一起

提交前请再检查：

- 是否残留模板欢迎页、演示命令、无用静态资源、占位逻辑或未来规划代码
- 是否把 Tauri 能力集中收口，而不是在多个位置散落调用
- 是否把关键说明同步到文档或记忆文件
- 是否至少通过与改动范围相匹配的检查命令

## 命名与组织建议

- Svelte 组件文件使用 `PascalCase.svelte`
- 特性目录使用 kebab-case，例如 `mochi-avatar`
- 服务文件按能力命名，例如 `system-monitor.ts`
- 动画模型、系统快照、展示状态的命名应体现层次，不要混用同一组概念
- Rust 模块名、Tauri 命令名和前端服务方法名尽量保持语义一致
- 文档命名优先使用能直接表达主题的中文名或英文能力名，避免 `misc`、`temp2`、`新建文档` 之类的模糊命名

## 验证与检查

常用命令：

```bash
pnpm dev
pnpm check
pnpm tauri dev
pnpm build
```

```bash
cargo check
```

验证原则：

- 前端结构或类型改动，至少运行 `pnpm check`
- 前端构建相关改动，必要时运行 `pnpm build`
- Rust / Tauri 侧改动，至少运行 `cargo check`
- 涉及窗口行为、透明背景、置顶、拖拽、吸附等平台相关能力时，明确说明已验证范围和未验证风险
- 纯映射逻辑、状态变换逻辑优先写成易测试的纯函数，便于后续补充测试

## Git 提交规范

如果要求 AI 生成提交信息，遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

- `feat:` 新增功能
- `fix:` 修复问题
- `docs:` 文档变更
- `refactor:` 重构代码但不改变外部行为
- `test:` 测试相关修改
- `chore:` 构建、配置、依赖或杂项维护

示例：

- `feat(desktop): 增加桌宠窗口吸附与位置恢复`
- `fix(avatar): 修复高负载状态下表情抖动异常`
- `docs(memory): 初始化 MochiDesk 项目记忆骨架`

## 特殊规则

- 不提前引入与当前阶段无关的技术选型，例如 monorepo 拆分、移动端壳、复杂插件体系、过度设计的共享包
- 不保留模板欢迎页、演示命令、无用依赖、未使用静态资源和无实际调用的桥接逻辑
- 新增 Tauri plugin、capability 或窗口特权前，必须确认存在真实使用场景
- 平台差异明显的能力必须注明验证状态，例如透明窗口、置顶、点击穿透、拖拽吸附、屏幕边界修正
- 文档内容必须描述 MochiDesk 当前状态，不保留和本项目无关的旧项目说明

## 项目记忆与协作规约

本项目使用 `docs/memory/` 目录构建 AI 的跨会话长期记忆，确保在多次协作中始终掌握最新的目标、约束、决策和进度。

### 记忆目录结构

| 路径 | 用途 | 命名规则 |
|------|------|----------|
| `docs/memory/index.md` | 全局索引：项目愿景、技术栈、模块状态、硬约束、阶段目标 | 单文件 |
| `docs/memory/待办.md` | 动态看板：Backlog / Todo / Doing / Done | 单文件 |
| `docs/memory/决策/` | 重大技术选型和明确结论 | 按主题命名 |
| `docs/memory/方案/` | 尚未完全落地的设计草案 | 按主题命名，落地后删除或转决策 |
| `docs/memory/会话纪要/` | 按主题累计的会话摘要与阶段结论 | 按主题命名 |
| `docs/plans/` | 正式计划文档、阶段拆解或里程碑草案 | 按主题命名 |

### AI 工作流（Read -> Think -> Act -> Sync）

| 阶段 | 动作 |
|------|------|
| `Read` | 每次对话开始先读取 `index.md` + `待办.md`；涉及具体模块时按需检索 `决策/`、`方案/` 和 `会话纪要/` |
| `Think` | 设计前先确认已有决策和当前阶段边界；引用旧代码只记结论和路径，不复制大段无关内容 |
| `Act` | 开始实作前，在 `待办.md` 标注 Doing；做完后同步状态 |
| `Sync` | 会话结束前：更新 `待办.md`、补充必要会话纪要、删除过时方案，并确保文档和实现一致 |

### 维护细则

- `待办.md` 中的 `Done` 项目超过 10 条时，归档到对应会话纪要并保留完成日期
- `决策/` 中同一主题优先在同一文件追加，建议使用【背景】【备选方案】【结论】【影响范围】格式
- `方案/` 只保留仍在评估或尚未落地的草案；一旦完成实现，应删除、归档或转为决策
- `会话纪要/` 记录阶段结果、约束变化和后续建议，不保存整段聊天原文
- 发现文档与代码不一致、内容过时或目录臃肿时，应主动清理并更新记忆

## 清理原则

- 模板欢迎页、演示命令、无用依赖、未使用静态资源属于优先清理项
- 文档内容应描述 MochiDesk 当前状态，不保留和本项目无关的旧项目说明
- 对未来方向的探索素材、临时 HTML 草图、视觉实验稿要和正式实现分开管理，避免误当成产品代码
